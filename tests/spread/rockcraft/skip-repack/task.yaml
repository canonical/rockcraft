summary: Check if repacking is skipped

execute: |
  rm -f *.rock
  rockcraft pack 2>&1 | MATCH "Packed"
  test -f skip-repack-test-*.rock
  rockcraft pack 2>&1 | MATCH "Skipping pack"
  rm -f *.rock
  rockcraft pack 2>&1 | MATCH "Packed"
  rm -f *.rock

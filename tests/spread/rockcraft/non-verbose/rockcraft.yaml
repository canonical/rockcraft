name: non-verbose-test
version: latest
base: ubuntu@24.04
build-base: devel
summary: A rock having pebble without --verbose argument
description: A rock having pebble without --verbose argument
license: Apache-2.0
services:
  non-verbose-service:
    override: replace
    command: sh -c "pwd; sleep 5"
    startup: enabled
    on-success: shutdown
    on-failure: shutdown
    working-dir: /tmp

platforms:
  amd64:

parts:
  part1:
    plugin: nil

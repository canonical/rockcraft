[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "test-poetry"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
black = "^24.8.0"
python = "^3.10"

[tool.poetry.dev-dependencies]

[tool.poetry.scripts]
test-poetry = "test_poetry.__main__:main"

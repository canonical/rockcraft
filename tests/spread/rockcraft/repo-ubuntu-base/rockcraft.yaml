name: apt-repo-test
version: latest
summary: A rock that pulls in a package from an external PPA
description: A rock that pulls in a package from an external PPA
license: Apache-2.0
base: ubuntu@22.04

platforms:
  amd64:

package-repositories:
  - type: apt
    ppa: deadsnakes/ppa
    priority: always
    # TODO: we declare the key here but craft-archives should support querying the
    # ppa's key through a proxy (OpenStack)
    key-id: F23C5A6CF475977595C89F51BA6932366A755776

parts:
  hello:
    plugin: nil
    stage-packages:
      - python3.12

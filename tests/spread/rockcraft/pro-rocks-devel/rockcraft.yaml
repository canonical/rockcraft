name: bare
summary: A minimal project file for executing lifecycle commands.
description: A rock containing only The Bare Necessities 
version: "latest"
license: Apache-2.0

base: devel
build-base: ubuntu@22.04

platforms:
  amd64:  

parts:
  bare:
    plugin: nil

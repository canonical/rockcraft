summary: ubuntu base cutoff test

restore: |
  run_rockcraft clean --destructive-mode

execute: |
  run_rockcraft prime --destructive-mode
  find /lib/ -name "libc.so.6" -print -quit | grep libc.so.6
  test $? -eq 1

summary: distroless build test

execute: |
  rockcraft pack

  test -f distroless-test_latest.rock
  test ! -d work

  # test container execution
  docker images
  sudo /snap/rockcraft/current/bin/skopeo --insecure-policy copy oci-archive:distroless-test_latest.rock docker-daemon:distroless-test:latest
  docker images
  docker run --rm distroless-test

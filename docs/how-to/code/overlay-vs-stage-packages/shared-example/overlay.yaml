name: ca-certs-example
summary: ca-certs example.
description: ca-certs example.
version: "0.1"
base: ubuntu@20.04
license: Apache-2.0
platforms:
  amd64:

parts:
  apt-dependencies:
    plugin: nil
    overlay-packages:
      - ca-certificates
    stage-packages:
      - curl

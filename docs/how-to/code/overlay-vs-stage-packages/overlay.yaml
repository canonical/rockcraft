name: ca-certs-example
summary: ca-certs example.
description: ca-certs example.
version: "0.1"
base: ubuntu@24.04
platforms:
  amd64:

parts:
  install-curl:
    plugin: nil
    overlay-packages:
      - ca-certificates
    stage-packages:
      - curl

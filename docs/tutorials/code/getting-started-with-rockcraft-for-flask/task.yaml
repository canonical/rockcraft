###########################################
# IMPORTANT
# Comments matter!
# The docs use the wrapping comments as 
# markers for including said instructions 
# as snippets in the docs.
###########################################
summary: Getting started with Rockcraft for Flask tutorial

environment:

execute: |
  # [docs:create-venv]
  sudo apt-get update && sudo apt-get install python3-venv -y
  python3 -m venv .venv
  source .venv/bin/activate
  pip install -r requirements.txt
  # [docs:create-venv-end]

  # [docs:run-flask]
  flask run &
  curl localhost:5000
  kill $!
  # [docs:run-flask-end]

  # [docs:create-rockcraft-yaml]
  rockcraft init --profile flask-framework
  sed -i "s/name: .*/name: flask-hello-world/g" rockcraft.yaml
  ls rockcraft.yaml
  # [create-rockcraft-yaml-end]

  # [docs:pack]
  sudo iptables -I DOCKER-USER -j ACCEPT
  rockcraft pack
  ls *.rock
  # [pack-end]

  # [docs:skopeo-copy]
  sudo rockcraft.skopeo --insecure-policy copy oci-archive:flask-hello-world_0.1_amd64.rock docker-daemon:flask-hello-world:0.1
  # [docs:skopeo-copy-end]

  sudo docker images flask-hello-world:0.1

  # [docs:docker-run]
  sudo docker run -d -p 8000:8000 --name flask-hello-world flask-hello-world:0.1
  curl localhost:8000
  # [docs:docker-run-end]

  # [docs:cleanup]
  sudo docker stop flask-hello-world
  sudo docker rm flask-hello-world
  sudo docker rmi flask-hello-world:0.1
  deactivate
  rm -rf .venv
  rm flask-hello-world_0.1_amd64.rock rockcraft.yaml app.py requirements.txt
  # [docs:cleanup-end]

name: chisel-hello
summary: Hello world from Chisel slices
description: A "bare" rock containing hello binaries from Chisel slices.
license: Apache-2.0

version: "1.0"
base: bare
build_base: "ubuntu@22.04"
platforms:
  amd64:

parts:
  hello:
    plugin: nil
    stage-packages:
      - hello_bins

###########################################
# IMPORTANT
# Comments matter!
# The docs use the wrapping comments as 
# markers for including said instructions 
# as snippets in the docs.
###########################################
summary: test for the "Install packages slices into a rock" tutorial

execute: |
  # [docs:install-rockcraft]
  snap install rockcraft --classic
  # [docs:install-rockcraft-end]

  # [docs:build-rock]
  rockcraft pack
  # [docs:build-rock-end]
  
  test -f chisel-hello_1.0_amd64.rock

  # [docs:skopeo-copy]
  sudo /snap/rockcraft/current/bin/skopeo --insecure-policy copy oci-archive:chisel-hello_1.0_amd64.rock docker-daemon:chisel-hello:latest
  # [docs:skopeo-copy-end]

  # [docs:docker-run]
  docker run --rm chisel-hello:1.0 exec hello -t
  # [docs:docker-run-end]
  
  docker run --rm chisel-hello:1.0 exec hello -t | grep "hello, world"
